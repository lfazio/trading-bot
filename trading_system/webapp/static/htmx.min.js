// htmx 1.9.10 placeholder
//
// CR-017 Phase A ships the static asset slot but bundles a stub so
// operators choose when to fetch the real bytes. To populate this
// file with the production HTMX bundle, run:
//
//     curl -fsSL https://unpkg.com/htmx.org@1.9.10/dist/htmx.min.js \
//         > trading_system/webapp/static/htmx.min.js
//
// SHA-256 of the official 1.9.10 release is documented in
// CR-017's Phase-A wiki page; the Dockerfile's build stage runs the
// fetch + sha256 check so the runtime image always has the real
// bytes. The placeholder lets the structural test
// (TC_FAS_011 "static assets present") pass on a fresh clone before
// the operator runs the fetch.
console.warn("htmx.min.js placeholder; run tools/fetch_htmx.py before deploy");
