// htmx-sse 1.9.10 placeholder — Server-Sent Events extension for HTMX.
//
// CR-017 Phase B uses ``hx-ext="sse"`` to subscribe to the
// ``/events/live-state`` channel. To populate this file with the
// production bytes, run (or use the operator fetch script):
//
//     curl -fsSL https://unpkg.com/htmx.org@1.9.10/dist/ext/sse.js \
//         > trading_system/webapp/static/htmx-sse.min.js
//
// SHA-256 of the official release is documented in CR-017's Phase-B
// wiki page. The bundled placeholder lets the structural test
// (TC_FAS_011) confirm the file exists; production deploys SHALL
// run the fetch step before the container ships.
console.warn(
  "htmx-sse.min.js placeholder; run tools/fetch_htmx.py before deploy"
);
