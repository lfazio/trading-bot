# trading-bot

Production-grade Python trading system optimizing **after-tax** returns under
French CTO / PFU taxation (30 % flat). Manages EU dividend & swing stocks,
tactical positions, and turbo / CFD leveraged instruments — scaling capital
through six gated phases, from a configurable starting amount up to
multi-million portfolios. **Broker-agnostic** by design; the lifecycle ships
an in-process `LocalBrokerAdapter` (deterministic simulation of fills, fees,
slippage) as the reference baseline. Live-broker adapters are deferred until
a broker is selected.

The repository is structured for engineering discipline first
(DO-178C-inspired lifecycle, full requirement traceability) and trading
performance second. It is a personal-use system, not financial advice or a
redistributable product.

---

## Project status

Lifecycle phases (gated; each is reviewed and approved before the next opens):

| Phase | Artifact | Status |
|---|---|---|
| 1 — Software Requirements (SRS) | [Wiki › SRS](https://github.com/lfazio/trading-bot/wiki/SRS) | Approved at `7424909`; re-approved 16× through 2026-05-15 (CR cascades) |
| 2 — System Design (SDS) | [Wiki › SDS](https://github.com/lfazio/trading-bot/wiki/SDS) | Approved at `26ce913`; re-approved with every cascade |
| 3 — Detailed Design (SDD) | [Wiki › SDD](https://github.com/lfazio/trading-bot/wiki/SDD) | Approved at `9ee11d5`; re-approved with every cascade |
| 4 — Test Plan | [Wiki › Test-Plan](https://github.com/lfazio/trading-bot/wiki/Test-Plan) | Approved at `2127a35`; re-approved with every cascade |
| 5 — Implementation | `trading_system/` | **Phase-5 queue closed 2026-05-15.** Modules shipped: persistence (CR-008), regime detector (CR-013), fundamentals provider (CR-014), trade rationale audit trail (CR-015), currency hedger (CR-011); 1149 tests green |
| 6 — Test Execution | `tests/` | Continuous — 1149 passing |
| 7 — Validation & Traceability | [`docs/traceability.csv`](./docs/traceability.csv) + [`Documentations/Traceability.md`](./Documentations/Traceability.md) | Auto-generated by `tools/traceability-report.py` |

**431 requirements** are tracked across SRS / SDS / SDD / Test Plan.
Current cumulative coverage: `reached TEST: 53% · CODE: 66% · TP: 100%`.
Run `python3 tools/traceability-report.py --report` for the live snapshot,
or browse [`Documentations/Traceability.md`](./Documentations/Traceability.md)
for the auto-generated requirements-by-status report.

### Change-request cascades complete (2026-05-15)

The lifecycle now reflects the operator-accepted change-request batch:

- **CR-008 (persistence layer)** — ✅ Done. SQLite + WAL + thin-mapper repositories;
  Connection / MigrationRunner / 6 repositories; `account_id` columns ready for
  CR-006 Phase 6; HMAC-gated registry promotion; bit-identical backtest archive.
- **CR-009 (Yahoo Finance backtest data feed)** — ✅ Done (2026-05-08).
- **CR-010 (sector rotation, Phase 5)** — ✅ Done (2026-05-14); now consumes
  CR-013's detector output.
- **CR-013 (regime detector + transition trigger)** — ✅ Done. Pure detector
  + transition tracker + orchestrator that raises a `KillSwitchTrigger`
  through the SafetyLayer and persists the event through CR-008.
- **CR-006 (multi-account) / CR-001 (notifications) / CR-004 (web UI) /
  CR-002 (quant hypothesis layer)** — 🟡 Design cascade complete; code lands
  in Phase 6.
- **CR-014 (fundamentals provider, Phase 5)** — ✅ Done. CSV-seeded EU
  universe + `CompositeFundamentalsProvider` for "yfinance OHLCV + CSV
  fundamentals" wirings; the Phase-5 dividend-aristocrat backtest now
  runs end-to-end.
- **CR-015 (trade rationale audit trail, Phase 5)** — ✅ Done.
  `TradeRationale` frozen dataclass + `BacktestResult.rationales` field
  + `analytics.rationale_for(...)` lookup; closed gate-name vocabulary +
  `validate_gate_vocabulary` audit helper; round-trips through CR-008.
- **CR-011 (currency hedging, Phase 5)** — ✅ Done. `wealth_ops/fx_hedger/`
  ships the pure exposure calculator + threshold-gated proposal emitter +
  append-only forward ledger with deterministic mark + EUR-tax treatment.
  Closes the Phase-5 implementation queue.

---

## Repository layout

```
trading-bot/
├── Documentations/             ← submodule → trading-bot.wiki.git
│   ├── Home.md                 ← wiki landing page
│   ├── SRS.md                  ← Software Requirements Specification
│   ├── SDS.md                  ← System Design Specification
│   ├── SDD.md                  ← Software Design Description
│   ├── Test-Plan.md            ← Test Plan
│   ├── Traceability.md         ← auto-generated REQ-status report
│   └── Change-Requests.md      ← proposed evolutions (CR-001..CR-015)
├── trading_system/             ← Phase-5 production code
│   ├── models/                 ← Result/Option + value types
│   │   └── rationale.py        ← TradeRationale audit row (CR-015)
│   ├── result.py               ← Option[T] / Result[T, E] stdlib union
│   ├── config/                 ← system / phases / risk / kill_switch /
│   │                              sectors / turbos YAML loaders
│   ├── data/                   ← MarketDataProvider Protocol + adapters
│   │   ├── mock.py             ← deterministic mock provider
│   │   ├── yfinance/           ← CR-009 backtest-only Yahoo Finance feed
│   │   └── fundamentals/       ← CR-014 CSV-seeded fundamentals provider
│   ├── persistence/            ← CR-008 SQLite system-of-record (WAL)
│   │   ├── connection.py       ← only sqlite3 caller; PRAGMA-pinned
│   │   ├── migrations/         ← 0001_init.sql, 0002_regime.sql, …
│   │   └── repositories/       ← Portfolio / Registry / Backtest /
│   │                              KillSwitchSnapshot / Transition
│   ├── safety/                 ← kill switch + StateManager + recovery
│   │                              (HMAC operator tokens)
│   ├── regime/                 ← CR-013 detector + transition tracker +
│   │                              orchestrator (raises STRATEGY trigger)
│   ├── phase_engine/           ← phase resolution from equity + injections
│   ├── capital_flow/           ← injection ledger
│   ├── tax/                    ← France-CTO 30 % engine + harvester
│   ├── risk/                   ← gate-ordered pre/post-trade engine
│   ├── screener/               ← dividend / yield / FCF / D/E filters
│   ├── strategies/             ← Core + Tactical + Ensemble (Phase 6)
│   ├── turbo_selector/         ← phase-gated knockout-distance scorer
│   ├── structured_products/    ← decomposer + admission gate (cap ≤ 10 %)
│   ├── milestone_controller/   ← 2k / 5k / … / 5M EUR ladder
│   ├── portfolio/              ← cash / positions / attribution
│   ├── portfolio_manager/      ← CR-005 read-only proposal generators
│   │                              (Rebalancer, SectorRotatorFacade,
│   │                              TaxHarvesterFacade) + attribution
│   ├── backtesting/            ← deterministic engine + walk-forward
│   │   └── result.py           ← BacktestResult + rationales tuple
│   ├── execution/              ← LocalBrokerAdapter (in-process)
│   ├── strategy_lab/           ← offline meta-loop (generator → registry)
│   │   └── quant/              ← CR-002 Hypothesis layer (5-gate validator
│   │                              + overfitting helpers + in-memory library)
│   ├── wealth_ops/             ← Phase-5: tax harvest, sector rotation,
│   │                              fx_hedger (CR-011)
│   ├── accounts/               ← CR-006 Phase-6 foundation:
│   │                              Account / AccountRegistry / PortfolioGroup /
│   │                              TaxModel / cross-account gate / household
│   │                              drawdown trigger / AccountScopedTokenVerifier
│   ├── analytics/              ← perf / exposure / drawdown / rationale_for
│   ├── notifications/          ← CR-001 channels + fan-out + approval gate
│   │                              + summary publisher (LocalLogChannel,
│   │                              NotificationFanOut, ApprovalGate)
│   ├── webui/                  ← CR-004 stdlib HTTP API — WebAuth,
│   │                              canonical JsonResponse schemas,
│   │                              InMemoryIdempotencyStore, Router +
│   │                              ThreadingHTTPServer + reference routes
│   ├── observability/          ← structured logging — JsonLineFormatter,
│   │                              LogContext, log_scope (REQ_SDS_CRS_001)
│   ├── config/                 ← centralised YAML validator —
│   │                              validate_all() drives every shipped
│   │                              loader; CLI: python -m trading_system.config
│   │                              --validate-all (REQ_SDS_CFG_001)
│   ├── dashboard/              ← read-only views (no trade actions)
│   └── main.py                 ← end-to-end runnable demo
├── tests/                      ← pytest suite (1549+ tests)
├── config/                     ← operator YAMLs
│   ├── system.yaml             ← starting capital, broker, base currency
│   ├── phases.yaml             ← per-phase constraint table
│   ├── risk.yaml               ← risk bands, drawdown caps
│   ├── kill_switch.yaml        ← KS trigger thresholds
│   ├── sectors.yaml            ← CR-010 regime → sector bias
│   └── turbos.yaml             ← turbo selector scoring weights
├── data/                       ← bundled seed data
│   └── seed_fundamentals.csv   ← CR-014 EU dividend universe (14 rows)
├── docs/
│   └── traceability.csv        ← REQ ↔ artifact matrix (generated)
├── tools/
│   ├── traceability-report.py  ← REQ scanner / drift gate + report
│   │                              (writes docs/traceability.csv +
│   │                               Documentations/Traceability.md)
│   ├── fundamentals_csv_template.py  ← CR-014 stub generator
│   └── yfinance_recorder.py    ← CR-009 cache-recorder script
├── trading-bot.md              ← original imported specification
├── TASKS.md                    ← engineering work breakdown
├── CLAUDE.md                   ← guidance for Claude Code agents
├── Makefile                    ← lint / typecheck / test / trace targets
├── pyproject.toml              ← Python 3.13+; optional [yfinance] extra
└── README.md                   ← this file
```

Phase-5 implementation is **in progress** — the modules listed above are the
shipped slices through 2026-05-16. Stubs / unshipped phases remain as
placeholders; the [`Traceability` wiki page](https://github.com/lfazio/trading-bot/wiki/Traceability)
shows which REQs have reached `CODE` / `TEST`. CR-006 (Phase 6 multi-account)
landed its foundation slice 2026-05-16 — `trading_system/accounts/` is
additive: runtime wiring (tick fan-out, per-account tax dispatch, household
drawdown subscriber, persistence call-site rewrites) is the next follow-up.

---

## Getting started

### Clone with the wiki submodule

```bash
git clone --recurse-submodules git@github.com:lfazio/trading-bot.git
cd trading-bot

# or, after a plain clone:
git submodule update --init --recursive
```

### Inspect the design

The lifecycle documents live in the wiki — browse them on GitHub or
locally under `Documentations/`. The
[Wiki Home page](https://github.com/lfazio/trading-bot/wiki) is the
recommended entry point.

### Verify traceability

```bash
python3 tools/traceability-report.py --report   # human-readable summary
python3 tools/traceability-report.py --check    # CI gate; exit 1 on drift
```

### Edit documentation

Edit pages in `Documentations/` as ordinary markdown. The submodule is a
separate git repo; commits there go to the wiki, not the main repo.

```bash
cd Documentations
# edit SRS.md / SDS.md / SDD.md / Test-Plan.md / Home.md
git add . && git commit -m "Refine REQ_F_TAX_006 wording"
git push origin master

cd ..
git add Documentations && git commit -m "Bump wiki to <sha>"
git push
```

---

## Hard rules

From the SRS and approved design documents — non-negotiable, apply to every
phase:

1. **Optimize net after-tax return.** Never gross. Backtests apply the
   30 % CTO / PFU tax and simulate the capital-injection timeline.
2. **Tax-aware trade gate.** A trade is valid only if
   `expected_net_profit > 5 × total_fees` *after tax*.
3. **Phase-gated lifecycle.** SRS → SDS → SDD → Test Plan → Code →
   Tests → Validation. No phase skipped; each is reviewed and approved
   before the next opens. Any change after approval restarts the
   lifecycle from the affected phase.
4. **Kill switch is non-bypassable.** Priority:
   `KillSwitch > RiskEngine > Strategy > Execution`. Trading stops on
   trip; recovery requires explicit operator confirmation.
5. **Bounded research engine.** The meta-optimization loop runs offline;
   the runtime imports only the read-only strategy registry.
6. **Configurable, never hard-coded.** Starting capital, broker
   selection, phase thresholds, risk limits — all from `config/*.yaml`.
7. **Survival > return.** Stopping incorrectly is preferred to trading
   incorrectly.

The full ruleset, including agent-specific guidance, is in
[`CLAUDE.md`](./CLAUDE.md).

---

## Contributing

Changes follow the lifecycle:

- **Spec or design changes** edit the relevant page in
  `Documentations/` and require re-approval (a new commit appended to
  the document's *Approval* section, with date, reviewer, and main-repo
  SHA).
- **Code changes** (Phase 5 onward) reference the requirement IDs they
  implement in module docstrings; tests reference the requirement IDs
  they verify. The traceability tool's `--check` mode runs in CI and
  blocks merges on drift, unknown REQ references, or duplicate
  definitions.
- **Tasks / tickets** live as
  [GitHub Issues](https://github.com/lfazio/trading-bot/issues) — one
  parent issue per phase, child issues per module or bug, labels
  `phase-N` / `module-X` / `bug` / `enhancement`.

---

## License

Released under the BSD 3-Clause License — see [`LICENSE`](./LICENSE) for
the full text. The license applies to all files in this repository and
to the documentation in the `Documentations/` submodule.
